"""Shared UI assets for every Oubliette web surface — currently `tokens.css`,
the single house-style token block both the play app and the Forge serve as
/tokens.css. Python-empty on purpose: this package exists so the css ships
with the package data."""
